# Workfare – Änderungen vom 14. September 2026

## Git und Umfang

Die Installationsreparatur ist separat lokal committed: `a6f739eb85301d470962e93cd2a2ad7bb3ba0559` – **Update dependencies and resolve install warnings**. Autor und Committer: **r-yalcin**, mit der dem GitHub-Konto zugeordneten Noreply-Adresse. Der Commit wurde in dieser Runde nicht gepusht.

Dieses Protokoll beschreibt den anschließenden, separat geprüften UI- und Bereinigungsstand für den lokalen Commit. Keine Veröffentlichung und kein neues Komplettdesign. Die vorhandene Brückengrafik wird wiederverwendet; keine neue Bildgestaltung wird eingeführt. Farbwelt, Schriftfamilien, Hintergrundeffekte, Produktkarten und Homepage-Reihenfolge bleiben Grundlage. Die Desktop-Aurora ist technisch optimiert. Die neue Browser-, Build- und Lifecycle-Prüfung ist unten dokumentiert; eine Verbesserung der Netzwerk-Ladezeit wurde nicht gemessen.

## Installation – separat committed

| Before | After |
|---|---|
| `npm install` beendet sich erfolgreich, meldet aber zehn betroffene Pakete: 1 kritisch, 6 hoch, 3 mittel. | Sauberes `npm ci`, normales `npm install` und `npm audit` erfolgreich; **0 bekannte Schwachstellen** zum Prüfzeitpunkt. Ein tatsächlicher Installationsabbruch war hier nicht reproduzierbar. |
| Next und Next-ESLint 16.2.6. | Beide aufgelöst auf 16.3.5, sichere Untergrenze ^16.3.3. |
| Festes PostCSS-Override 8.5.14 verhindert aktuelle Korrekturen. | Override entfernt; Next löst PostCSS 8.5.23 auf. |
| Ältere Unterabhängigkeiten, einschließlich Sharp. | Kompatible Aktualisierungen, insbesondere Sharp 0.35.4 und baseline-browser-mapping 2.11.23; plattformspezifische Compilerpakete bleiben erhalten. |

Geändert wurden für diesen Commit ausschließlich `package.json` und `package-lock.json`. Kein erzwungenes Audit-Upgrade und keine neuen Hauptversionen. Getestet mit Node 24.11.0 und npm 11.11.1.

Quellen zur Versionsentscheidung: [Next-Korrektur](https://github.com/vercel/next.js/security/advisories/GHSA-2xp9-vwfh-vxw4), [Next 16.3.5](https://github.com/vercel/next.js/releases/tag/v16.3.5), [PostCSS](https://github.com/postcss/postcss/security/advisories/GHSA-fxqj-rqcc-2cmp), [Sharp](https://github.com/lovell/sharp/security/advisories/GHSA-rgj7-g3m4-5g8c).

## Header, Menü und Text

| Before | After |
|---|---|
| Startseite hat eigenen Header und eine zweite umfangreiche Menüimplementierung in `animated-hero.tsx`. | Startseite verwendet denselben `SiteHeader` und `SiteMenu` wie die entsprechenden Unterseiten. Rund 300 Zeilen doppelte Hero-Navigation entfernt. |
| JobBridge-Schriftzug und zwischenzeitlich als Ersatz gezeichnetes Monoline-W. | Workfare-Schriftzug in der vorhandenen Geist-Schrift; `BrandMark.tsx` verwendet wieder `/jobbridge-bridge-logo-white.png`. Das Brückenzeichen ist 29 × 18 Pixel groß und mit `-top-px` optisch einen Pixel angehoben. |
| Unterschiedliche Logo-/Menümaterialien, zwischenzeitlich 16 Pixel Radius und farbige Glaseffekte. | Gemeinsame `.glass-control`/`.glass-button` mit `999px` Rundung, klarer Mitte, neutralen weißen Reflexen, schmaler Lichtkante und schwachem Schatten. Kein künstlicher Blauverlauf und kein Sättigungsfilter im Glasmaterial. |
| Headerglas und sekundärer Hero-Button unabhängig gestaltet. | Gemeinsames Material aus `src/app/globals.css`: Steuerflächen 44 × 44 Pixel, Glasbutton mindestens 54 Pixel hoch; `blur(1.5px) brightness(1.04)`, sichtbarer Fokus und Fallback ohne Blur-Unterstützung. |
| Die erste lokale Vereinfachung entfernte die ursprüngliche Menübewegung und Iconrotation. | Ursprüngliches Verhalten wiederhergestellt: Menü-Slide beim Öffnen/Schließen, gestaffelte Linkeinblendung, Word-Roll bei Hover/Fokus und Hoverrotation der Menü-/Schließen-Icons. Reduced Motion reduziert beziehungsweise deaktiviert diese Bewegung. |
| Einfache Links ohne ursprünglichen Word-Roll in der Zwischenfassung. | Zwei versetzte Textzeilen rollen beim Hover/Fokus; die zweite Zeile ist für assistive Technik verborgen. Linkbezeichnung, aktive Seite und Tastaturfokus bleiben ausdrücklich benannt. |
| Große Menüwortmarke und Meta-Links separat angeordnet. | Gemeinsamer `.hero-menu-footer` im normalen Dokumentfluss: Nebenlinks über einer zurückhaltenden, unten angeschnittenen Workfare-Signatur am Fensterrand. Responsive Größen und Abstände für kleine Bildschirmhöhen. |
| Selbstgebautes Overlay sperrt Scrollen, hat aber keine verlässlich begrenzte Tastaturreihenfolge. | Natives Dialogelement, Scrollsperre, explizite Tab-Schleife, Escape und Rückkehr zum Menüknopf; `aria-expanded`, `aria-controls` und Dialogbezeichnung. |
| Navigation konnte unmittelbar mit dem Linkklick stattfinden. | Normaler Linkklick merkt das Ziel vor und führt es nach dem Schließ-Slide aus. Modifizierte Klicks behalten ihr natives Verhalten. Der Wechsel zur Sicherheitsseite nach dem Schließen ist im Browser geprüft. |
| Langer Werbeabsatz mit abstrakten Schutzbehauptungen. | „Ein bisschen helfen. Eigenes Geld verdienen. Mit Workfare findest du kleine Jobs in deiner Nähe und Menschen, die deine Unterstützung brauchen.“ |
| Hero-Aktionen verwenden pauschales `transition-all` und springen beim Hover nach oben. | Übergänge auf Fläche/Kante/Schatten begrenzt, ohne Hochspringen. Der primäre Button bleibt weiß; „So funktioniert's“ nutzt `.glass-button`. Reduced-Motion-Ausnahme ergänzt. |
| Dichtes Tracking und enger zusammengerückte Zeilen der Heroüberschrift. | Instrument Serif bleibt. `.hero-heading` setzt `letter-spacing: -0.015em` und `line-height: 1.06`; die zweite Zeile sitzt nach der Feinabstimmung mit `margin-top: -0.18em` wieder näher an der ersten. Schriftgröße und Buchstabenabstand bleiben dabei erhalten. |
| Frühere responsive Größenstaffelung des Hero. | Kräftigere responsive Größe: `clamp(3rem, 15vw, 8rem)`, ab 1024 Pixeln `clamp(4.5rem, 9vw, 8.4rem)`. In acht Größen von 320 bis 1920 Pixeln ohne horizontalen Überlauf geprüft. |
| Desktop-Aurora rendert mit bis zu 1,5-facher Pixeldichte. | CSS-Pixel-Auflösung mit etwa Full-HD-Pixelbudget; die weiche Aurora-Farbwelt bleibt erhalten. |
| Rendering folgt der Bildschirmfrequenz. | Zielrate 30 Bilder/s; zeitbasierte Bewegung behält das bisherige Tempo eines 60-Hz-Bildschirms. |
| Renderloop und Mausberechnung laufen auch außerhalb des Hero weiter. | Pause bei unsichtbarem Hero oder verborgenem Tab, kontrollierte Wiederaufnahme und vollständiges Aufräumen beim Unmount. |
| Der Shader berechnet einen ungenutzten Scroll-/Glow-Pfad. | Toten `glow`-/`uScroll`-Code und zugehörige Anbindung entfernt. Die Vordergrund-Parallaxe bleibt. |
| Hero-Absatz kann kurze Restzeilen bilden. | `text-pretty` gleicht den Umbruch des bestehenden Absatzes aus. |
| Aktuelle zentrale Metadaten, Footer und Chat-Bezeichnungen nennen JobBridge. | Diese aktuellen Markenstellen beziehen Workfare aus der zentralen Konfiguration. Historische Artikel, Anbieter-/Personendaten und funktionierende App-/Mailadressen bleiben erhalten. Weitere aktuelle Altmarkenstellen sind noch separate Folgearbeit. |

## Code, Medien und Dokumentation

| Before | After |
|---|---|
| Vier nachweislich ungenutzte Quell-/Stildateien. | `LegalPage.tsx`, ausschließlich davon genutztes `LegalInteractions.tsx`, `shader-animation.tsx` und `src/styles/globals.css` entfernt. |
| Nicht mehr genutzte Legal-Highlight-, Accordion- und Shimmer-CSS-Regeln. | Nur diese belegten toten Regeln entfernt; aktive Shader/Scrollanimationen bleiben. |
| Ungenutzte `homePageConfig`-Flags und `getOwnInsight`. | Definitionen entfernt. |
| RootLayout dupliziert die JSON-LD-Escapefunktion. | Gemeinsamen vorhandenen Helfer importiert; Escaping und Roundtrip geprüft. |
| OG-Logo als 1024×1024 beschrieben, Bild tatsächlich 1200×630. | Metadaten auf 1200×630 korrigiert. |
| OG-Route nutzt die unter Next 16.3 veraltete Edge-Runtime. | Expliziten Edge-Export entfernt; Standard-Node-Runtime. Bild weiterhin 1200×630 PNG, Build ohne diese Warnung. |
| Freigabe für Unsplash in Bildkonfiguration und CSP, obwohl keine Quelle verwendet wird. | Beide nicht benötigten Freigaben entfernt; aktive lokale Medien funktionieren weiter. |
| Neues Next erzeugt beim lokalen Start automatisch AGENTS.md/CLAUDE.md. | Automatische Erzeugung mit dokumentiertem `agentRules: false` deaktiviert und ausschließlich die neu erzeugten Vorlagendateien entfernt. |
| Vier nicht referenzierte öffentliche Bildkopien, zusammen 1.147.898 Byte. | Entfernt: `blick-aktuell-jugend-forscht-bundesfinale.jpg`, `rezan-yalcin-profile-jugend-forscht.jpg`, `sg-rheinbach-bundeswettbewerb.jpg`, `wdr-jobbridge-programmieren.jpg` unter `public/insights/`. Dynamische Seiten, Sitemap und Feed enthalten keine Verweise auf diese Dateien. |
| Generische README für nicht existierende Pages-Router-Dateien. | Tatsächliche Projektstruktur, Installations-/Prüfschritte, lokale Veröffentlichungstrennung und Medienregeln dokumentiert. |
| Keine vollständige Medien-/Dateiübersicht. | `file-audit.md` klassifiziert alle 81 ursprünglichen Dateien; `image-provenance.md` dokumentiert die 17 ursprünglichen Bilder und ihre Belege. `design-plan.md` enthält die nächsten gezielten Verbesserungen am bestehenden Design. |

## Bilder und Google

Bei den vorhandenen Dateien ließ sich **kein generatives KI-Bild belegen**. Canva-Exportdaten beweisen ein gestaltetes Cover, nicht generative KI. Ein WDR-Bild trägt Screenshot-Metadaten; ein Pressefoto enthält Kamera-/Fotografenangaben. Fehlende Metadaten beweisen weder echte Fotografie noch KI. Deshalb wurden verwendete Bilder und ihre Rechtehinweise nicht pauschal entfernt.

Die vier entfernten Dateien waren ungenutzt, nicht nachweislich KI-generiert. Die Löschung ist bisher nur lokal. Nach einer späteren Veröffentlichung liefern ihre bisherigen Website-Pfade keine Datei mehr; eine vorhandene Google-Indexierung verschwindet dadurch nicht sofort. In dieser Runde wurde kein Search-Console-Löschauftrag durchgeführt. Die verworfenen neuen Bildentwürfe wurden nicht in das Repository oder `public` übernommen.

## Vorheriger Prüfstand – vor den jüngsten UI- und Aurora-Korrekturen

Die folgenden Ergebnisse wurden an einer früheren lokalen Fassung erzielt. Sie dokumentieren die damals gelaufenen Prüfungen, **keine abschließende Abnahme** der jetzt wiederhergestellten Menüanimation, der Menüfooter-Anordnung, der Brücke, des runden neutralen Glases, der Hero-CTAs, der geänderten Serifüberschrift oder der laufenden Aurora-Optimierung.

- Produktionsbuild einschließlich TypeScript erfolgreich: 33 generierte Seiten.
- ESLint, SEO-Prüfung (16 statische Seiten) und `git diff --check` erfolgreich.
- 31 bestehende Domainprüfungen gegen lokalen Produktionsserver erfolgreich.
- Startseite, `/sicherheit` und ein eigener Artikel: HTTP 200 und HTML.
- `/og-image.png`: HTTP 200, `image/png`, 1200×630 Pixel nach Runtime-Umstellung.
- Browser: Header/Headline bei 320×568, 375×667, 390×844, 430×932, 768×1024, 1024×768, 1440×900 und 1920×1080. Kein horizontaler Dokumentüberlauf, Überschrift ohne horizontalen Überlauf, beide sichtbaren Glaselemente jeweils 44×44.
- Menü: Öffnen, Tab/Shift-Tab an beiden Enden, Escape, Fokus-Rückkehr, Scrollfreigabe und Navigation zur Sicherheitsseite geprüft. Kleine Handyhöhe und Desktopdarstellung visuell geprüft.
- Nach Paketwechsel tauchte im noch alten Browserzustand einmal `validationLevel` auf. Nach Serverneustart und Neuladen damals keine Wiederholung; kein entsprechender Projektcode. Am Ende dieses früheren Prüflaufs war der temporäre Produktionsserver auf Port 3100 beendet und die Entwicklungsansicht auf Port 3000 aktiv. Daraus wird kein aktueller Serverzustand abgeleitet.

## Neue Abschlussprüfung – bestanden

- Abschließender ESLint-Lauf und Produktionsbuild einschließlich TypeScript erfolgreich: 33 Seiten generiert.
- Neue responsive Prüfung bei 320×568, 375×667, 390×844, 430×932, 768×1024, 1024×600, 1440×900 und 1920×1080: keine horizontale Überbreite von Dokument oder Heroüberschrift. Unter 1024 Pixeln bleibt der mobile Hintergrund ohne Aurora-Canvas.
- Screenshots für kleine und normale Handygrößen sowie Desktop geprüft: Brücke optisch einen Pixel angehoben, neutrale Glasreflexe und stärkere Serifüberschrift. Bei geringer Höhe darf der Inhalt nach unten scrollen, statt die Schrift zu verkleinern.
- Menü-Slide beim Öffnen und Schließen im Browser beobachtet; Meta-Links stehen vollständig oberhalb der dekorativen Wortmarke. Auf 320×568 passt das komplette Menü ohne horizontalen oder vertikalen Überlauf.
- Tab und Shift-Tab laufen zwischen Schließen und E-Mail im Dialog; Escape schließt und gibt Fokus sowie Scrollen zurück. Navigation zur Sicherheitsseite nach dem Schließen und Rückkehr zur Startseite geprüft. Ursprüngliche Word-Roll-/Hoverrotationswerte und Reduced-Motion-Ausnahmen im Quellcode geprüft.
- Beide Hero-CTAs haben im Browser keine Translation/Transformation; ihre Übergänge betreffen nur Hintergrund und Schatten. Der Glasbutton scrollt zur Erklärung; Rückkehr zum Hero zeigt den Hintergrund wieder korrekt.
- Desktop-Canvas bei 1440×900: 1406×868 Renderpixel statt einer zusätzlich erhöhten Pixeldichte. Achtung: keine gemessene GPU-Zeit oder Netzwerk-Ladezeitverkürzung.
- Temporärer Lifecycle-Test des tatsächlichen Aurora-Effects: 13 Prüfgruppen bestanden, einschließlich Pause/Resume, verstecktem Tab, fehlendem WebGL, verspätetem Import, Unmount und Ressourcenfreigabe. Bei simulierten 60/120/144 Hz jeweils 299 Renderaufrufe in zehn Sekunden, gleiche Bewegungsgeschwindigkeit. 4K-Host verwendet 1920×1080 Renderpixel. DOM/Three-Mocks prüfen Ablauf und Renderplanung, keine GPU-Treiber oder echte Bildrate.
- Ein veralteter Entwicklungs-Stylecache wurde separat gesichert und neu aufgebaut; neue Schriftstile werden seitdem korrekt angezeigt. Die lokale Vorschau läuft auf Port 3000. Die aktuelle Browser-Fehlerliste ist leer.
- `git diff --check` erfolgreich. Zu diesem Prüfzeitpunkt noch kein zusätzlicher Commit, kein Push und keine Veröffentlichung. Der Abschluss für den freigegebenen lokalen Commit folgt unten.

Dies ist keine vollständige WCAG-, Lizenz-, Sicherheits- oder Gerätefreigabe. Die detaillierte Dateiinventur nennt Prüfgrenzen. Offene Inhalte, übrige aktuelle Altmarkenstellen, Favicon und weitere kleine UI-Befunde stehen im Folgeplan. Es wurde kein vollständiger Markenwechsel oder kompletter Designumbau veröffentlicht.


## Zweite Feinabstimmung: Hierarchie und Zeilenabstand

| Before | After |
|---|---|
| Die beiden Herozeilen stehen nach der ersten Korrektur zu weit auseinander. | `.hero-heading-typed` setzt `margin-top: -0.18em`: etwa 11 Pixel weniger Abstand bei 390 Pixel Bildschirmbreite. Buchstabenabstände und responsive Größen unverändert. |
| Große vollständige Menüwortmarke mit 12 Prozent Weiß konkurriert mit Hauptlinks. | Signatur mit 5,5 Prozent neutralem Weiß, Größe `clamp(4.5rem, 10vw, 8rem)`, auf kleinen Bildschirmhöhen 3–4,5rem. |
| Wortmarke sitzt über den Nebenlinks. | Nebenlinks zuerst, darunter die dekorative Signatur am unteren Fensterrand; im normalen Dokumentfluss, ohne Überlagerung. |
| Wortmarke ist vollständig sichtbar und vom Rand abgesetzt. | Höhe `.76em` mit bewusstem unteren Beschnitt. Die bedienbaren Links stehen oberhalb der Dekoration. In der letzten Feinabstimmung wurde der zusätzliche Footerabstand auf null reduziert, damit beide Elemente enger zusammenstehen. |

Desktop- und Handy-Screenshots geprüft. Menü bei 320×568, 390×844, 1024×600 und 1440×900 kontrolliert; bei beiden kurzen Formaten kein Dialogüberlauf, Nebenlinks vollständig über der Signatur. Heroabstand bei 390 Pixeln und normalem Desktopfenster visuell geprüft. ESLint beider berührter Komponenten, CSS-Parser und `git diff --check` bestanden. Kein erneuter Produktionsbuild für diese reine Abstands-/Anordnungsrunde; der vorherige erfolgreiche Build bleibt separat dokumentiert. Diese Zwischenprüfung fand vor der anschließenden Commit-Freigabe statt.


## Abschluss für den lokalen Commit

Nach der Freigabe wurden die Nebenlinks näher an die Workfare-Signatur gerückt: `.hero-menu-footer` verwendet `gap: 0`, die zuvor zusätzlichen 4–12 Pixel Abstand entfallen. Die dekorative Wortmarke bleibt am unteren Fensterrand, die klickbaren Links vollständig darüber. Hauptnavigation und Animationen bleiben erhalten.

Abschlussprüfung des gesamten Quelltextstands: ESLint fehlerfrei, SEO für 16 statische Seiten bestanden, Produktionsbuild einschließlich TypeScript erfolgreich mit 33 generierten Seiten. Die responsive und funktionale Browserprüfung ist oben dokumentiert. Autor und Committer sind auf das GitHub-Profil `r-yalcin` mit der zugehörigen Noreply-Adresse konfiguriert. Dieser Abschluss umfasst einen lokalen Commit; Push und Veröffentlichung sind kein Teil dieses Schritts. Die Folgearbeiten in `design-plan.md` gehören zur nächsten inhaltlichen Runde.
